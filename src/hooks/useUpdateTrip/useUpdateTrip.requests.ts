import { UpdateTripRequestValues } from './types'

export const useUpdateTripRequests: Record<string, UpdateTripRequestValues> = {
    validRequest: {
        tripName: undefined,
        imageRef: 'image-2',
        startDate: undefined,
        endDate: undefined
    }
}